class streamTracker():
    def __init__(self, stream):
        self.d = {}
        for integer in stream:
            self.track(integer)

    def track(self, integer):
        if num in self.d:
            self.d[num]
        for num in self.d:
def trackFromStream(stream):
